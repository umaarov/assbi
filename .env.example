# Copy this file to `.env` and fill in your real key.
#   cp .env.example .env        (PowerShell: Copy-Item .env.example .env)
# The app reads .env automatically on startup. A real exported env var wins
# over this file, so either approach works.

DEEPSEEK_API_KEY=sk-REPLACE_ME_WITH_YOUR_KEY
