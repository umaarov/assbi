# Lightweight stack — the BI dashboard, CLI, reporting and chatbot.
# This is what Streamlit Community Cloud installs (no torch/opencv needed:
# the cloud app serves the recorded warehouse; the Live Monitor's heavy
# imports are lazy and only used locally — see requirements-full.txt).

pyyaml>=6.0
pandas>=2.0
streamlit>=1.30
openpyxl>=3.1          # Excel workbook in the Power BI export pack
requests>=2.31         # DeepSeek / OpenAI-compatible chatbot calls
